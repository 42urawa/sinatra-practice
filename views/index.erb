<% content_for :title do %>
  トップページ
<% end %>

  <button onclick="location.href='/new'" class="add">+Add Memo</button>
  <ul class="container">
    <% @memos.each do |memo| %>
    <li>
      <a href="/memos/<%= memo["id"].to_s %>">
        <%= Rack::Utils.escape_html(memo["title"]) %>
      </a>
    </li>
    <% end %>
  </ul>
